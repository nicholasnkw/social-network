<section class="col-2-3">
  <h1><%= @profile.first_name %>&nbsp<%= @profile.last_name %></h1>
  <%= gravatar_for @user %>
  <h3><%= @profile.blurb %></h3>
  <% if current_user == @user%>
  <%= link_to "edit profile", edit_user_profiles_path(:user_id => current_user.profile.id) %>
  <% end %>
</section>
<div class=" col-1-3 clearfix" > 
  <h2>Friends</h2>
  <% if current_user == @user%>
    <ul>
      <% if @friends %>
        <% @friends_profiles.each do |f| %>
      <li><%=link_to "#{f.first_name } #{f.last_name}", user_path(:id => f.user_id) %> </li>
        <% end %>
      <% end %>
    </ul>
  <% end %>
</div>